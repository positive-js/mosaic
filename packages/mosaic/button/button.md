Mosaic buttons are native `<button>` or `<a>` elements.

<!-- example(button-overview) -->

There are five button variants, each applied as an attribute:

| Attribute          | Description                                                                 |
|--------------------|-----------------------------------------------------------------------------|
| `mc-button`        | Rectangular button w/ no elevation.                                         |

### Theming

### Accessibility